

	// let text =  "fan cu"
	// let content = "waffan culo";
	// 	console.log(content.split(text).join(`<bold>${text}</bold>`))
		

// 		handleClick(e, openTag, closeTag) {


// onClick={ (e) => this.handleClick(e, "<i>", "</i>")



 
function search (value) {
	let text = ["waffanculo", "pidr", "cu later"];
	let searchResults=[];
		 text.forEach(function(curr) {
			
            if (curr.search(value) !== -1) {
                searchResults.push(curr)
            } 
		    
            
            // cross adevent listener searchresults=""
 			//flash content
 			// componentn unmount - clear state

        })
return searchResults
        }


console.log(search("щ"))